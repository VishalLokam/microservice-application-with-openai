{
  "name": "ecommerse-microservices",
  "version": "1.0.0",
  "description": "open the `ai-service` folder\r 1. Install python dependencies by running the below command\r     ```\r     pip install --no-cache-dir -r requirements.txt\r     ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}
